// config.js

/**
 * Configuration settings for the Cursed City project.
 */

const config = {
    // Maximum number of buildings in Nodeberg
    MAX_BUILDINGS: 100000,

    // Any other configuration constants can be added here
};

module.exports = config;
